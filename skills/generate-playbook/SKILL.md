---
name: generate-playbook
description: Generate or refresh a 1ops playbook for the current repo. Detects dev command, port, and seed file, writes /.ops/playbook.yaml (pointers only — never secrets), and symlinks it into the 1ops playbooks dir. Use when onboarding a repo to 1ops or when its dev/staging/prod access details have drifted.
---

# Generate a 1ops playbook

Your job: produce `/.ops/playbook.yaml` in the **current repo**, conforming to the
1ops contract, then symlink it so the dashboard can find it. Playbooks store
**pointers, never secrets** — if you are about to write a password, an API key,
or a token value, stop: write the *source* instead (a seed file path or a
1Password item name).

## Steps

1. **Identify the app.** Use the repo folder name as `app` unless `package.json`
   `name` is clearly better. One playbook per repo.

2. **Detect the dev story** (don't guess — read the files):
   - `start`: the dev script in `package.json` (`scripts.dev`), or the framework's
     convention (`next dev`, `vite`, `flutter run`, etc.).
   - dev `url`: infer from `PORT` in `.env` / `.env.example` / the dev script;
     default to the framework's default (Next → `http://localhost:3000`).
   - dev `accounts`: find the seed file (`prisma/seed.ts`, `seed.*`, `db/seed*`,
     a `db:seed` script). If found → `source: seed`, `seedFile: <relative path>`.
     If the app has no auth in dev → `source: inline-nonsecret` with a short note.
     **Never copy credential values out of the seed file** — just point at it.

3. **Detect staging & prod** (best effort, then ask):
   - Look for URLs in `README`, `vercel.json`, CI config, `.env.example` comments.
   - For `accounts`, use `source: vault` with a `vaultItem` name. Ask the user for
     the exact 1Password item name if you can't infer a sensible one. Never invent
     a credential.

4. **Capture gotchas.** Pull setup landmines from the README ("run docker compose
   up first", "needs X env var"). Keep them one line each.

5. **Write the committed file `/.ops/playbook.yaml`** using the schema in
   `docs/SCHEMA.md`. This holds the MAXIMUM safe-to-publish info: commands +
   their folders, URLs, the seed file pointer, seeded dev users (public), and
   integration names/dashboards. Add a header comment noting it is generated by
   this skill and stores pointers only.

6. **Protect the sensitive overlay.** Anything genuinely sensitive that the user
   still wants on the dashboard goes in `/.ops/playbook.local.yaml`, which is
   **never committed**. Ensure the repo ignores it (append if missing):
   ```sh
   grep -q '.ops/*.local.yaml' .gitignore 2>/dev/null || printf '\n# 1ops: sensitive overlay, never commit\n.ops/*.local.yaml\n' >> .gitignore
   ```
   Only create `playbook.local.yaml` if there is actually something sensitive to
   put there — most apps won't need one (seeded dev users are public).

7. **Symlink into the playbooks dir** so the dashboard sees it. Symlink the base;
   the dashboard auto-discovers the `.local.yaml` overlay next to the real target:
   ```sh
   mkdir -p "${ONEOPS_PLAYBOOKS_DIR:-$HOME/playbooks}"
   ln -sf "$(pwd)/.ops/playbook.yaml" "${ONEOPS_PLAYBOOKS_DIR:-$HOME/playbooks}/<app>.playbook.yaml"
   ```

8. **Verify before declaring done.** Re-read the COMMITTED file and run the
   secret tripwire mentally (private keys, `AKIA…`, `ghp_…`, `sk-…`, JWTs). If any
   real secret slipped into the committed file, move it to `playbook.local.yaml`.
   Confirm the symlink resolves: `readlink "${ONEOPS_PLAYBOOKS_DIR:-$HOME/playbooks}/<app>.playbook.yaml"`.

## Output

Report: the path written, the symlink created, and a one-line summary per env
(`dev → localhost:3000 (seed: prisma/seed.ts)`). Flag anything you had to guess
so the user can correct it.
